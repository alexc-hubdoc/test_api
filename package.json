{
  "name": "test_api",
  "version": "0.0.1",
  "description": "My test api",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "nodemon --watch .",
    "postgres": "docker-compose up -d",
    "postgres-cli": "psql -h localhost -p 2346 -U db_developer -d api_db"
  },
  "keywords": [
    "express",
    "node"
  ],
  "author": "alex-ac2",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.18.2",
    "uuid": "^7.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
